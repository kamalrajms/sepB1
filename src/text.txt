31/08/2026

## react intro

    What is React?
  

  
    1) React is a JavaScript library (not a framework) developed by Facebook (Meta) for building user interfaces (UIs), especially Single Page Applications (SPAs).

    2) It helps developers build reusable UI components that update efficiently when data changes.

    Instead of reloading a whole webpage when something changes, React updates only the parts of the UI that need to change.

    -------
    Why do we need React?

	It makes UI development faster, cleaner, and more efficient.

	Provides a better user experience with smooth updates without page reloads.


    ## must need to install node.js
 
## procedure to initiate project

    step 1 : npm create vite@latest [project_name]
    step 2 : select react
    step 3 : select javascript+ react compailer
    step 4 : npm install                // install node module packages
    step 5 : npm run dev 

    if you want to re-run your project mean 
        npm run dev

01/09/2026

## Props

    Props is nothing but drilling data from parent component to 
    child component.

            declearing Props
            DeStructuring props

## conditional rendering

    conditional rendering is means showing  or hiding part of UI 
    based on some condition (state,props...)

                ternary operator  {condition ? true:flase}
                Logical operator  && || !

2/9/2026

## list Rendering 

    Rendering multiple item dynamically using .map()

    arr=[1,2,3,4]

    arr.map((num)=>(
            num*2
    ))

    update arr=[2,4,6,8]


## style in react

    global style -- app.css and index.css is called as global style file  
    Local style -- we need to create a seperate css  file for a component
    module style -- to implement this module style , we need to create seperate css file 
                    and we need to mention the file name as eg: [filename.module.css]
    inline style -- we need to declear a style attribute in {{padding:"20px"}}
    object style -- we need to declear  a styleing property in an obj formate as {key:value}
                    and we need to call obj name in style attribute.


03/09/2026  && 7/9/2026

## useState()

    useState is a react hook that allows you to add a state (data that can change over a time)
    to a functional component.

    Normally in js function , variables are disappear after the function run,
    but in with useState , react remeber the value b/w renders.


    syntax:

        const num=55  --- js file

        const[state,setState]=useState(initialValue)
        const[count,setCount]=useState(7)


        state --  the current  value of the state variables
        setState -- a function that update the state
        initialValue -- the starting value of the  state. 


08/09/2026

## useEffect

    In react, useEffect is a hook that lets you to perform side effect in functional
    component.
    Side effect are task that happend ouside the normal react re-rendering process
    Like:

                fetching the data from an API
                Updating the browser DOM manually
                starting/ stoping timers


    syntax:

        useEffect(()=>{
            //code  function
        },[count,dependency 2......])

        [] ---> if you don't mension any dependency means 
                it will execute only once at begining.


## useRef()


    useRef is a react hook that gives you a box (.current) you can store anything in
    -like= num,str,obj,function,even DOm elements..

    That value persists b/t rendering

    Updating it dose not cause your component to re-render.

    {current:value}
    

    syntax:

        const variables=useRef(initialValue)


10/9/2026

## useContext()

    Passing data from a parent component down to deeply nested child componentis annoying.
    Normally, you'd have to pass props manually though each level called props drilling.
    useContext lets you skip all the midddle component and share data directly.

            create a context
            Provide a value
            Consume the value

    createContext should be declear only once in a component.


## Domain

    a domain name is the human readable addressused to 
    accesss a website on the internert.

    it rep-IP address

    eg:     example.com



    Sub domain

        mail.example.com
        drive.example.com
        career.example.com

    Different types of domain

    .com -- commercial
    .org -- organization
    .net -- network
    .edu -- education
    .gov -- government
    .mil -- military
    .info -- information
    .biz -- business
    .in -- india
    
