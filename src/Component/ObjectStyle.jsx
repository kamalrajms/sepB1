import React from "react";

export default function ObjectStyle() {
  const theme = {
    backgroundColor: "yellow",
    color: "red",
    padding: "20px",
  };
  return (
    <div style={theme}>
      <h2>object style</h2>
    </div>
  );
}
